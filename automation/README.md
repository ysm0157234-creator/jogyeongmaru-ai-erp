# 국립종자원 생산·수입판매 신고 자동입력

ERP가 만든 ZIP을 그대로 써서 [국립종자원 종자민원서비스](https://www.seednet.go.kr)의
품종 생산·수입판매 신고 화면을 자동으로 채운다. **신고(제출) 버튼은 누르지 않는다.**
내용을 확인한 뒤 사람이 직접 누른다.

## 자동화하지 않는 것 (의도적으로)

| | 이유 |
|---|---|
| **로그인** (아이디·비밀번호, 공동인증서) | 자격증명은 자동화가 대신 입력하지 않는다. 브라우저 창에서 직접 로그인한다. |
| **신고(제출) 버튼** | 되돌릴 수 없는 행정 행위다. 사람이 확인하고 누른다. |

로그인은 한 번 하면 세션이 `automation/.seednet-session.json`에 저장되어 다음 실행 때는 생략된다.
(이 파일에는 로그인 쿠키가 들어 있으므로 git에 올리지 않는다.)

## 설치

```bash
cd ~/Documents/GitHub/jogyeongmaru-ai-erp/automation
pip3 install -r requirements.txt
python3 -m playwright install chromium
```

> 아래 명령은 모두 **`automation` 폴더 안에서** 실행한다.
> (저장소 루트에서 실행하면 `No module named 'seednet'` 오류가 난다.)

## 1단계 — 신고 화면 구조 뽑기 (최초 1회)

신고 작성 폼은 로그인 안쪽이라 미리 볼 수 없다. 추측으로 선택자를 짜면 틀리므로
실제 화면 구조부터 뽑는다.

```bash
python3 -m seednet.dump_form
```

브라우저가 열리면 → 로그인 → 신청서 목록에서 **`신고서작성하기`** 클릭.

신고 화면은 여러 단계로 나뉘어 있을 수 있으므로, **화면이 바뀔 때마다** 터미널에서
Enter를 눌러 캡처한다. 다 끝나면 `q`를 입력한다.
`automation/form_dump.json`에 화면별 입력 항목이 저장된다.

## 2단계 — 입력 매핑 (작성 완료)

`automation/field_map.json`이 이미 들어 있다. 2026-08-19 기준 신고서작성 화면
(`cvwAppMstReg.do?appl_knd=30`)에 맞춰져 있다.

화면이 개편되면 1단계를 다시 돌려 `form_dump.json`을 새로 뽑고 이 파일의
`selector`만 고치면 된다.

### 자동으로 채우는 항목 (13개)

생산/수입 구분(수입), 수입국가, 작물 일반명, 작물 학명, 품종 영문명,
종자제출 방식(확약서 첨부), 육성자 여부(아니오), 권리취득 방법(기타)과 기타 내용,
종자업 등록번호, 수입식물 검역합격증명서 번호, 개인정보 동의 2건

### 건드리지 않는 항목

신고인 성명·대표자·생년월일·전화번호·주소는 **로그인 계정 정보로 이미 채워져** 나오므로
덮어쓰지 않는다.

### 직접 해야 하는 항목

| 항목 | 이유 |
|---|---|
| 작물 검색 | `[검색]` 팝업에서 골라야 작물분류(`crop_gubun`)가 채워진다 |
| 품종 한글명 | 품종명의 한글 표기가 ERP에 없다. 입력 후 `[중복확인]` 필요 |
| 품종특성기술서 | `[품종특성기술서 입력]` / `[특성표 입력]` 팝업 |
| 파일첨부 | 품종사진·검역서류·육성과정·종자업등록증은 `[파일첨부]` 팝업에서 올린다 |
| **제출** | `[종자원 접수요청]` — 확인 후 직접 누른다 |

## 직원 PC 설치 (윈도우)

1. 저장소 폴더를 직원 PC에 복사한다 (또는 압축을 풀어 준다)
2. `automation\windows\설치.bat` 을 두 번 누른다
   - 파이썬이 없으면 안내가 뜬다. [python.org](https://www.python.org/downloads/)에서 설치하되
     **"Add Python to PATH" 를 반드시 체크**한다
   - 브라우저(크로미움)를 내려받는 데 처음 한 번 몇 분 걸린다
   - 끝나면 바탕화면에 **[신고 도우미]** 아이콘이 생긴다
3. 바탕화면의 **[신고 도우미]** 를 실행한다. 검은 창이 뜨면 켜진 것이다. **닫지 않는다.**
4. **Chrome**으로 ERP에 접속해 **[신고 자동입력]** 버튼을 누른다

맥은 `automation/mac/설치.command` → `도우미 켜기.command` 순서로 같은 방식이다.

### 종자원 로그인

회사 계정 하나를 공유하므로, **PC마다 처음 한 번만** 열린 브라우저에서 로그인하면 된다.
그 뒤로는 저장된 세션을 재사용한다. 아이디·비밀번호는 도우미가 다루지 않는다.

### 자주 묻는 것

| 증상 | 해결 |
|---|---|
| 버튼을 눌러도 "도우미에 연결하지 못했습니다" | 검은 창(도우미)이 켜져 있는지 확인. Chrome을 쓰는지 확인 |
| "이미 실행 중입니다" | 정상이다. 이미 켜져 있으니 그대로 두면 된다 |
| 로그인 화면이 계속 뜬다 | 세션이 만료된 것이다. 한 번 더 로그인하면 다시 저장된다 |

## 웹앱 버튼으로 실행하기 (권장)

신고 자동화는 **로그인된 브라우저가 있는 이 컴퓨터에서** 돌아야 한다. Render 서버는
그 세션을 가질 수 없다. 그래서 맥에서 작은 도우미를 띄우고, 웹앱 버튼이 그것을 부른다.

```bash
cd ~/Documents/GitHub/jogyeongmaru-ai-erp/automation
python3 -m seednet.local_server
```

이 창을 켜 둔 채로 ERP 웹앱에서 **[신고 자동입력]** 버튼을 누르면 된다.

> **Chrome으로 열어야 한다.** Safari는 https 페이지에서 `http://127.0.0.1` 호출을 막는다.
> Chrome은 localhost를 예외로 허용한다.

- 처음 한 번은 열린 브라우저에서 직접 로그인한다. 이후에는 세션이 저장되어 생략된다.
- 도우미는 `127.0.0.1`에서만 받는다. 외부에서 접근할 수 없다.
- 아이디·비밀번호는 도우미가 다루지 않는다.

## 터미널에서 실행하기 (매번)

이 사이트는 **임시저장을 해야 첨부 기능이 열린다.** 그래서 실행이 두 단계로 나뉜다.

```
① 입력 자동 채움  →  ② [임시저장] 직접 클릭  →  ③ 첨부 자동  →  ④ [종자원 접수요청] 직접 클릭
                        (스크립트가 기다림)              (스크립트가 멈춤)
```

```bash
python3 -m seednet.fill_report "~/Downloads/수국_ENDLESS_SUMMER_complete.zip"
```

- ZIP의 `manifest.json > report_fields`에서 신고 값을 읽는다
- 신고서 `.hwp`, 검역합격증명서, 인보이스를 첨부한다
- 채운 항목과 **직접 입력해야 하는 항목**을 터미널에 정리해 보여준다
- 브라우저를 열어둔 채 끝난다 → 확인 후 직접 신고 버튼을 누른다

## 주의

- 값이 비어 있는 항목은 건너뛰고 목록으로 알려준다. 특히 **검역합격 발급번호**는
  Drive에서 검역합격증을 못 찾으면 빈 값이 된다.
- 자동 입력 결과를 그대로 믿지 말고 제출 전에 화면을 확인한다.
